// Runtime environment overrides for the frontend.
// This file is optional and can be served by your hosting platform (e.g., via static hosting or a rewrite)
// to change the API endpoint at runtime without rebuilding the app.
// Example usage on your server: serve a file with the following contents (replace URL):
// window._env_ = { REACT_APP_BACKEND_URL: 'https://your-backend.example.com' };

window._env_ = window._env_ || {};
// window._env_.REACT_APP_BACKEND_URL = 'https://nstrack-backend-2.onrender.com';
